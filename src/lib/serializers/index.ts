/**
 * Serializers: Internal (short) → External API (verbose).
 * Use only in API routes when the route sends data to an external API.
 * Add per-resource, e.g.:
 * export { serializeUser, serializeCreateUser, serializeUpdateUser } from "./user-serializer";
 */
