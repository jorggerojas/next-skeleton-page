/**
 * Normalizers: External API (verbose) → Internal (short).
 * Use only in API routes when the route calls an external API.
 * Add per-resource, e.g.:
 * export { normalizeUser, normalizeUserList } from "./user-normalizer";
 */
